---
title: Project Structure
description: Annotated directory tree for the sast-link-next repository
---

```
sast-link-next/
├── app/                        # Next.js App Router root
│   ├── layout.tsx              # Root layout: applies Geist font, wraps with Providers
│   ├── providers.tsx           # SWR config, ThemeProvider, conditional MSW bootstrap
│   ├── globals.css             # Tailwind v4 CSS variables and base styles
│   ├── page.tsx                # Root page: account switcher / quick-login entry
│   ├── (tourist)/              # Route group: unauthenticated pages (no shared auth layout)
│   │   ├── layout.tsx          # Shared gradient background for all tourist pages
│   │   ├── login/              # Two-step login flow
│   │   │   ├── page.tsx
│   │   │   └── _components/    # login-step-1.tsx, login-step-2.tsx
│   │   ├── register/           # Four-step registration flow
│   │   │   ├── page.tsx
│   │   │   └── _components/    # register-step-1.tsx through register-step-4.tsx (+ step-3 test)
│   │   ├── reset/              # Four-step password reset flow
│   │   │   ├── page.tsx
│   │   │   └── _components/    # reset-step-1.tsx through reset-step-4.tsx
│   │   └── callback/
│   │       ├── feishu/page.tsx # Feishu/Lark OAuth callback handler
│   │       └── github/page.tsx # GitHub OAuth callback handler
│   └── (user)/                 # Route group: authenticated pages
│       └── home/
│           ├── layout.tsx      # Fetches profile (useFetchProfile), renders TopBar + parallel panels
│           ├── page.tsx        # Homepage: profile summary, quick actions, apps grid
│           ├── @infoPanel/     # Parallel route: side info panel
│           ├── @profilePanel/  # Parallel route: side profile panel
│           └── edit/
│               ├── layout.tsx
│               ├── page.tsx    # Avatar cropping + profile fields (nickname, bio, links)
│               └── safety/
│                   └── page.tsx # Safety settings: email display, future rebinding
│
├── components/                 # Shared UI and feature-oriented components
│   ├── account/                # AccountPanel: account switcher widget
│   ├── animation/              # PageTransition: Framer Motion page transitions
│   ├── auth/
│   │   ├── auth-shell.tsx      # Layout container for auth pages (gradient bg, width constraint)
│   │   ├── auth-form-field.tsx # Accessible input with label, description, suffix, error state
│   │   ├── other-login-list.tsx # OAuth provider buttons (GitHub, Feishu, QQ, Microsoft)
│   │   └── verification-code-input.tsx # Email code input with 60-second resend cooldown
│   ├── feedback/
│   │   └── global-message-panel.tsx # Sonner toast integration for global messages
│   ├── icons/
│   │   ├── logo.tsx            # Application logo SVG
│   │   └── brand-icons.tsx     # OAuth provider SVG icons
│   ├── layout/
│   │   ├── top-bar.tsx         # Header bar with navigation and user info
│   │   ├── footer.tsx          # Page footer
│   │   ├── back-layout.tsx     # Back-button layout wrapper for nested pages
│   │   └── theme-toggle.tsx    # Dark/light mode toggle (next-themes)
│   ├── navigation/
│   │   └── back-button.tsx     # Back navigation button
│   ├── profile/
│   │   └── bind-app-item.tsx   # Displays a bound OAuth application entry
│   └── ui/                     # Primitive components (shadcn/ui-style)
│       ├── button.tsx
│       ├── card.tsx
│       ├── input.tsx
│       ├── label.tsx
│       ├── avatar.tsx
│       ├── badge.tsx
│       ├── dialog.tsx
│       ├── separator.tsx
│       ├── sheet.tsx
│       ├── dropdown-menu.tsx
│       ├── select.tsx
│       ├── slider.tsx
│       ├── form.tsx
│       ├── dot-loading.tsx
│       ├── magic-card.tsx
│       └── sonner.tsx
│
├── hooks/
│   └── use-fetch-profile.ts    # SWR hook: fetches profile, populates three Zustand stores
│
├── lib/
│   ├── api/
│   │   ├── types.ts            # Shared types: ResType<T>, UserProfileType, EditableProfileType, UserAccount
│   │   ├── client.ts           # Axios instance with base URL + Token header interceptor
│   │   ├── auth.ts             # Authentication API functions
│   │   ├── user.ts             # User and profile API functions
│   │   └── oauth.ts            # OAuth authorize URL builder
│   ├── validations/
│   │   ├── auth.ts             # Zod schemas for login, register, and reset forms
│   │   └── profile.ts          # Zod schema for profile edit form
│   ├── constants/
│   │   └── department.ts       # Department enumeration
│   ├── utils.ts                # cn() utility (clsx + tailwind-merge)
│   ├── token.ts                # localStorage token read/write/clear helpers
│   ├── message.ts              # Global message facade (wraps Sonner)
│   └── form-helpers.ts         # Form utility helpers
│
├── store/                      # Zustand stores
│   ├── use-auth-store.ts       # Auth state: currentUser, loginTicket, redirect; persisted
│   ├── use-user-list-store.ts  # Remembered accounts list; persisted
│   ├── use-user-profile-store.ts # Full UserProfileType for edit pages; not persisted
│   └── use-panel-store.ts      # UI state: homeInfoPanel open/close; not persisted
│
├── mocks/                      # MSW mock infrastructure
│   ├── index.ts                # initMocks(): conditionally loads browser or server worker
│   ├── browser.ts              # MSW browser worker setup
│   ├── server.ts               # MSW server setup for Jest
│   ├── data/                   # Static mock data
│   └── handlers/
│       ├── index.ts            # Combines all handlers
│       ├── auth.ts             # Mocks /verify/account, /sendEmail, /verify/captcha, register, reset, OAuth
│       └── user.ts             # Mocks /user/login, /user/info, /user/logout, profile endpoints
│
├── docs/                       # Standalone Fumadocs documentation site (this site)
│
├── next.config.ts              # Next.js config: static export and image remotePatterns
├── jest.config.ts              # Jest configuration: jest-fixed-jsdom, ts-jest, path aliases
├── jest.setup.ts               # Jest setup: Testing Library matchers, Next.js mocks, MSW server
├── jest.polyfills.ts           # Polyfills for the test environment
├── tsconfig.json               # TypeScript: strict mode, @/* path alias
└── package.json                # Dependencies and scripts
```

## Path alias

The TypeScript path alias `@/*` maps to the repository root. All internal imports should use this alias:

```ts
import { cn } from "@/lib/utils";
import { useAuthStore } from "@/store/use-auth-store";
import { Button } from "@/components/ui/button";
```
